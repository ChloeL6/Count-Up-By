//business
const countTo = 30; //this can change based on user input
const countBy = 3; //this can change based on user input
let numberArr = []; 
let result = 0;

for (let index = 1; result < countTo; index++) {
  //let total = countby * index 
  let total = countBy * index;
  result = countBy * index;
  //push value ^ to numberArr
  console.log(result)
  numberArr.push(total);
  numberArr;
}


















// choose 2 numbers
// number to count to: 10; user inputs
// number to count by: 2; user inputs
// output should count by 2 to reach 10 *maybe an array*
